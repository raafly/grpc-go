syntax = "proto3";
package grpc_server;

option go_package = "/pb";

service UserServ {
    rpc GetUserData(Empty) returns(Users);
}

message Empty {}

message Users {
    repeated User users = 1;
}

message User {
    uint64 id = 1;
    string name = 2;
    string email = 3;
}